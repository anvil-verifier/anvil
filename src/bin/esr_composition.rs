// Verification entry point for the rely/guarantee composition proofs in
// `verifiable_controllers::composition`. Scope verification with:
//     cargo verus verify esr_composition -- --verify-module composition --rlimit 50 --time

use verifiable_controllers::composition as _;

fn main() {}
